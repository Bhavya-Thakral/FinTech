const {Config} = require('@react-native-community/cli-types');

/** @type {Config} */
const config = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./src/assets/fonts'],
};

module.exports = config;
